#include "../include/server.h"

void            action_broadcast(t_server *server, t_client *current_client)
{
  (void)server;
  (void)current_client;
}

void            action_fork(t_server *server, t_client *current_client)
{
  (void)server;
  (void)current_client;
}

void      action_incantation(t_server *server, t_client *current_client)
{
  (void)server;
  (void)current_client;
}

void      action_connect_nbr(t_server *server, t_client *current_client)
{
  (void)server;
  (void)current_client;
}

void      action_push(t_server *server, t_client *current_client)
{
  (void)server;
  (void)current_client;
}