#include "../include/server.h"

void			action_inventary(t_server *server, t_client *current_client)
{
  (void)server;
  (void)current_client;
}

void			action_take_object(t_server *server, t_client *current_client)
{
  (void)server;
  (void)current_client;
}

void		        action_drop_object(t_server *server, t_client *current_client)
{
  (void)server;
  (void)current_client;
}

void			action_see(t_server *server, t_client *current_client)
{
  (void)server;
  (void)current_client;
}
